# laravel-env-phpstan-rule
Prevent env use outside config directory

# Installation

include rules.neon in your project's PHPStan config:

```
includes:
    - vendor/canvural/larastan-strict-rules/rules.neon
```
</details>
